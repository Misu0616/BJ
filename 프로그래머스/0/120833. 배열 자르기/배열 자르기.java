import java.util.*;

class Solution {
    public int[] solution(int[] numbers, int num1, int num2) {
        int[] answer = {};
        
        numbers = Arrays.copyOfRange(numbers, num1, num2+1);
        
        return numbers;
    }
}